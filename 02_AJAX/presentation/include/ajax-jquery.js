$.ajax({type: "GET",
        url: "http://[.....]",
        success: function(result) {
            /* Do something with result (using React). */
        },
        error: function(e) {
            /* Deal with error. */
        }
       }
      );
